<h1>List of foods to poop for <%= @pet.name %></h1>

<table>
  <thead>
    <tr>
      <th>Name of food</th>
      <th>Calories in food</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pet.foods.each do |food| %>
      <tr>
        <td><%= food.name %></td>
        <td><%= food.calories %></td>
        <td><%= link_to 'Poop this food', [@pet, food], method: :delete, data: { confirm: 'Are you sure?' }%></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Feed Pet new Food', new_pet_food_path(@pet, @food) %>
